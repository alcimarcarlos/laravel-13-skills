---
description: Use Laravel 13 Skills portable skills
globs:
  - "skills/**"
  - "docs/**"
  - "AGENTS.md"
  - "CLAUDE.md"
  - ".github/copilot-instructions.md"
alwaysApply: true
---

# Laravel 13 Skills

Use `skills/<skill-name>/SKILL.md` as the canonical skill instruction file.

Follow `AGENTS.md` first, then the smallest matching skill. Use `laravel-13-core` as the baseline when a task crosses multiple topics.

Do not duplicate skill bodies into Cursor rules. `.cursor/skills/<skill-name>` should remain an alias to `skills/<skill-name>`.
